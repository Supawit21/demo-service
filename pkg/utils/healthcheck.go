package utils

// var pool *pgxpool.Pool

// func HealthCheck() {
// 	ctx, cancel := context.WithTimeout(context.Background(), 2*time.Second)
// 	defer cancel()

// 	// Check the connection by pinging the DB
// 	err := pool.Ping(ctx)
// 	if err != nil {

// 	}
// }
